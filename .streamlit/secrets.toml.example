# Copy this file to .streamlit/secrets.toml for local use (that file is
# git-ignored and should NEVER be committed). On Streamlit Community Cloud,
# you don't create this file at all — you paste the same content into
# App settings -> Secrets in the dashboard instead.

GROQ_API_KEY = "gsk_your_key_here"
